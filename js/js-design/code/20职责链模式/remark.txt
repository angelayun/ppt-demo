职责链模式：解决请求的发送者与请求的接受者之间的耦合，通过职责链上的多个对象对分解请求流程，
实现请求在多个对象之间的传递，直到最后一个对象完成请求的处理


职责链模式定义了请求的传递方向，通过多个对象对请求的传递，实现一个复杂的逻辑操作，因此职责链模式将复杂的需求颗粒化逐一实现每个对象分内的需求，
并将请求顺序的传递。对象职责链上的每个对象来说，它都可能是请求的发起者也可能是请求的接收者。通过这种的方式不仅仅简化原对象的复杂度，而且解决原
请求的发起者与原请求的接收者之间的耦合